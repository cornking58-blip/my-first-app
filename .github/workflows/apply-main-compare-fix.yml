name: Apply main compare fix

on:
  push:
    branches:
      - agent/pr-80-fix-main-compare

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/pr-80-fix-main-compare
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Apply source patch
        run: python scripts/apply_main_compare_fix.py
      - name: Install Yarn 1
        run: npm install --global yarn@1.22.22 --silent
      - name: Install frontend dependencies
        working-directory: frontend
        run: yarn install --frozen-lockfile --silent
      - name: TypeScript check
        working-directory: frontend
        run: yarn exec tsc --noEmit
      - name: Commit working change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add 'frontend/app/(tabs)/index.tsx'
          git diff --cached --quiet || git commit -m "Fix main compare entry flow"
          git push
